import React, { Component } from 'react'

import './index.scss'

class Column extends Component {
  constructor(...props){
    super(...props)
  }

  render(){

    return (
      <div>
        专栏
      </div>
    )
  }
}

export default Column
